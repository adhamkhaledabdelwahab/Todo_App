const databaseFetchingPathErrMsg = 'Fetching database path fails!';

const databaseCreatingTaskTableErrMsg = 'Creating task table fails!';

const databaseCreatingNotificationTableErrMsg =
    'Creating notification table fails!';

const databaseInitializationErrMsg = 'Initialization of database fails!';

const databaseCreateTaskErrMsg = 'Create new task fails!';

const databaseFetchSingleTaskErrMsg = 'Fetch single task fails!';

const databaseDeleteAllTasksErrMsg = 'Delete all tasks fails!';

const databaseDeleteSingleTaskErrMsg = 'Delete single task fails!';

const databaseDeletingSingleTaskNotificationsErrMsg =
    'Delete single task notifications fails!';

const databaseFetchAllTasksErrMsg = 'Fetch all tasks fails!';

const databaseDeleteSelectedTasksErrMsg = 'Delete selected tasks fails!';

const databaseDeleteAllNotificationsErrMsg = 'Delete all notifications fails!';

const databaseDeleteSelectedNotificationsErrMsg =
    'Delete selected notifications fails!';

const databaseDeleteSingleNotificationErrMsg =
    'Delete single notification fails!';

const databaseFetchAllNotificationsErrMsg = 'Fetch all notifications fails!';

const databaseFetchCompletedTasksErrMsg = 'Fetch completed tasks fails!';

const databaseFetchFavouriteTasksErrMsg = 'Fetch favourite tasks fails!';

const databaseFetchUnCompletedTasksErrMsg = 'Fetch uncompleted tasks fails!';

const databaseMarkAllNotificationAsReadErrMsg =
    'Mark all notifications as read fails!';

const databaseMarkAllTasksAsCompletedErrMsg =
    'Mark all tasks as completed fails!';

const databaseMarkAllTasksAsFavouriteErrMsg =
    'Mark all tasks as favourite fails!';

const databaseMarkAllTasksAsUnCompletedErrMsg =
    'Mark all tasks as uncompleted fails!';

const databaseMarkAllTasksAsUnFavouriteErrMsg =
    'Mark all tasks as un favourite fails!';

const databaseFetchSelectedTasksErrMsg = 'Fetch selected tasks fails!';

const databaseMarkSelectedTasksAsCompletedErrMsg =
    'Mark selected tasks as completed fails!';

const databaseMarkSelectedTasksAsFavouriteErrMsg =
    'Mark selected tasks as favourite fails!';

const databaseMarkSelectedTasksAsUnCompletedErrMsg =
    'Mark selected tasks as uncompleted fails!';

const databaseMarkSelectedTasksAsUnFavouriteErrMsg =
    'Mark selected tasks as un favourite fails!';

const databaseFetchSingleNotificationErrMsg =
    'Fetch single notification fails!';

const databaseMarkSingleNotificationAsReadErrMsg =
    'Mark single notification as read fails!';

const databaseMarkSingleNotificationAsUnReadErrMsg =
    'Mark single notification as unread fails!';

const databaseMarkSingleTaskAsCompletedErrMsg =
    'Mark single task as completed fails!';

const databaseMarkSingleTaskAsFavouriteErrMsg =
    'Mark single task as favourite fails!';

const databaseMarkSingleTaskAsUnCompletedErrMsg =
    'Mark single task as uncompleted fails!';

const databaseMarkSingleTaskAsUnFavouriteErrMsg =
    'Mark single task as un favourite fails!';

const notificationInitTimeZonesErrMsg =
    'Initialization of notification time zones fails!';

const notificationIOSPermissionErrMsg = 'Requesting IOS permission fails!';

const notificationInitializationErrMsg =
    'Initialization of notification fails!';

const notificationWorkManagerInitializationErrMsg =
    'Initialization of notification work manager fails!';

const notificationCancelALlErrMsg = 'Cancel all notifications fails!';

const notificationCancelSingleErrMsg = 'Cancel notification fails!';

const notificationCancelSelectedErrMsg = 'Cancel selected notifications fails!';

const notificationScheduleErrMsg = 'Schedule notification fails!';

const notificationRepeatErrMsg = 'Repeat notification fails!';
